# StockBriefing AI — 설계 문서 (design.md)

> 작성일: 2026-05-30
> 상태: 설계 확정 (구현 전)
> 작성자: 1인 개발 / 취업 포트폴리오 (AI 엔지니어 신입 지원)

---

## 0. 한 줄 요약

매일 아침 8시, 밤사이 시장에서 터진 **공시·뉴스 중 "중요한 재료"만 LLM이 분류·요약**해 디스코드·텔레그램으로 자동 발송하는 **투자 의사결정 보조 도구**.

**포지셔닝 (중요):**
- ❌ "주가 예측 AI" (법적 리스크 + 면접 마이너스)
- ✅ "투자 의사결정 보조 도구" — 정보 수집 / 요약 / **중요도 분류**

---

## 1. 문제 정의 & 핵심 가치 제안

### 실제 사용자 = 개발자 본인 (단타 트레이더)
현재 아침 루틴의 페인 포인트:
- **밤사이 내 종목/관심권에 뉴스·공시가 떴는지 일일이 검색하는 노동**
- 주식 TV 프로그램 등 여러 채널에 흩어진 정보를 따로 챙겨봐야 함
- 단타 특성상 **매일 종목이 바뀜** → 고정 관심종목보다 "오늘 움직일 재료"가 중요

### 가치 제안
> **"밤사이 시장 재료를 일일이 검색하던 노동을 0으로."**

장 시작(9시) 전 8시에 **전체 시장 공시·뉴스 중 중요도 높은 재료만 분류된 리포트**를 받아, 그날 종목 후보를 1~2분 안에 파악. 기존 20~40분 검색·취합 → 읽기 1~2분으로 압축.

---

## 2. MVP 범위 (4주)

### ✅ In Scope (핵심 엔진: 입구 둘 / 엔진 하나)
| 기능 | 설명 |
|---|---|
| **B. 시장 촉매 레이더 (메인)** | DART 전체 공시를 시간창[전일 15:30 ~ 당일 08:00]으로 스캔 → 중요도 분류 → 재료 있는 종목 정리 |
| **A. 코어 관심종목 감시 (서브)** | 설정파일에 등록한 고정 종목에 동일 분석 적용 |
| **시장 지표 섹션** | 코스피/코스닥 지수, 원/달러 환율, 유가 (국내장 한정) |
| **자동 발송** | 평일 오전 8시, 디스코드 + 텔레그램 동시 발송 |
| **시크릿 분리** | `.env`(로컬) / GitHub Secrets(운영) |

### ❌ Out of Scope (MVP 제외 → 백로그)
- 미국장 지수 섹션 (국내장 집중)
- 주식 TV 프로그램 음성/영상 자동 요약 (난이도 ↑)
- KIS API 실시간 가격/등락률 (계좌+OAuth 부담)
- DB 영속화 (리포트 이력 저장) — stateless로 시작
- 웹 대시보드 / 사용자 인증 / 멀티 유저

---

## 3. "투자자문 아님" 설계 — 중립 큐레이션 모드

핵심 원칙: **종목을 평가하지 않고, "공시/뉴스의 시장 영향도"를 평가**한다. (정보 분류 ≠ 종목 추천)

| 하는 것 ✅ | 안 하는 것 ❌ |
|---|---|
| "이 종목에 OO 공시가 떴다" (사실) | "이 종목 사라/팔아라" (조언) |
| "공급계약 공시는 통상 재료로 작용" (일반 정보) | "오를 것이다 / 목표가 OO" (예측) |
| 공시 중요도 높음/중간/낮음 분류 | 종목 매수 순위 매기기 |
| 출처 링크 + 원문 인용 | 출처 없는 단정 |

**시스템 차원의 강제 장치:**
1. **금지어 가드레일** — LLM 출력에서 매수/매도/추천/목표가/오를것 등 차단 (프롬프트 규칙 + 후처리 필터)
2. **출처 의무** — 모든 항목에 DART/뉴스 원문 링크 부착
3. **고정 면책 문구** — 리포트 하단 항상 노출
4. **분류 대상 고정** — LLM이 평가하는 대상은 항상 "공시/뉴스"이지 "종목"이 아님 (프롬프트 스키마로 강제)

> 면접 멘트: *"법적 리스크를 면책 문구가 아니라 시스템 동작 규칙(분류 스키마 + 가드레일)으로 통제했다."*

---

## 4. 데이터 소스 (키 발급 완료 ✅)

| 우선순위 | 소스 | 용도 | 비고 |
|---|---|---|---|
| 1 | **DART OpenAPI** | 전체 공시 목록/상세 (촉매 핵심) | 무료, `list.json` 날짜범위 조회 |
| 2 | **네이버 검색 API** | 종목별 뉴스 보강 | `검색` API, Client ID/Secret |
| 3 | **한국은행 ECOS** | 원/달러 환율 등 거시지표 | 인증키 |
| 백로그 | KIS API | 전일 등락률/가격 | OAuth, 여유 시 |

**데이터 흐름 인사이트:** 8시는 장 시작 전이라 실시간 가격이 없음 → 가격 API(KIS)는 MVP 비필수. 공시(재료) + 뉴스(맥락) + 거시지표(분위기)만으로 완결된 리포트 가능.

---

## 5. 아키텍처

매일 1회 배치 파이프라인을 **오케스트레이터**가 조율. 특화된 컴포넌트 + LLM 추론 단계로 구성.

```
        ┌─────────────────────────────────────────┐
        │   Daily Briefing Orchestrator (08:00)   │
        └─────────────────────────────────────────┘
           │           │            │
   ┌───────▼────┐ ┌────▼─────┐ ┌────▼──────┐
   │ 공시 수집   │ │ 뉴스 수집 │ │ 시장지표   │
   │  (DART)    │ │ (네이버)  │ │  (ECOS)   │
   └──────┬─────┘ └────┬─────┘ └────┬──────┘
          │  ┌─────────┘            │
          │  │   ┌──────────────────┘
   ┌──────▼──▼───▼──────┐
   │ 코어종목 설정(YAML) │  ← A 기능 입구
   └──────┬─────────────┘
   ┌──────▼─────────────────────┐
   │ 중요도 분류 (Claude Sonnet) │  ← 가드레일 적용
   │  공시/뉴스 → 높음/중간/낮음 │
   └──────┬─────────────────────┘
   ┌──────▼─────────────────────┐
   │ 리포트 생성 (Claude)        │  ← 금지어 후처리 필터
   └──────┬─────────────────────┘
   ┌──────▼──────────┐
   │ 발송 추상화 레이어 │  (Notifier 인터페이스)
   └──┬───────────┬──┘
 ┌────▼────┐ ┌───▼─────┐
 │ 디스코드 │ │ 텔레그램 │
 │ Webhook │ │  Bot    │
 └─────────┘ └─────────┘

   [얇은 FastAPI 레이어] — 수동 트리거 / 최신 리포트 조회 / 헬스체크
```

### 컴포넌트별 책임
| 컴포넌트 | 책임 | 의존성 |
|---|---|---|
| **Orchestrator** | 전체 파이프라인 순서 제어, 에러 핸들링 | 모든 컴포넌트 |
| **DisclosureCollector** | DART 시간창 공시 수집·정규화 | DART API |
| **NewsCollector** | 종목 키워드로 뉴스 검색·중복제거 | 네이버 API |
| **MarketIndicator** | 환율·지수·유가 수집 | ECOS API |
| **WatchlistConfig** | 코어 종목 로딩 | `watchlist.yaml` |
| **ImportanceClassifier** | 공시/뉴스 영향도 분류 (LLM) | Claude Sonnet |
| **ReportBuilder** | 섹션 조립 + 금지어 필터 + 면책 | Claude |
| **Notifier (추상)** | `send(report)` 인터페이스 | Discord/Telegram 구현체 |
| **StorageInterface** | 리포트 저장 추상화 (MVP는 no-op/파일) | (향후 Postgres) |

> **상태(state) 설계:** B 기능은 시간창으로 공시를 조회하므로 **중복제거용 영속 상태가 불필요** → MVP는 stateless. GitHub Actions의 무상태 실행 환경과 완벽히 부합. `StorageInterface`만 깔끔히 두어 향후 Postgres(리포트 이력) 추가가 trivial.

---

## 6. 데이터 플로우 (1회 실행)

1. **08:00** GitHub Actions cron 트리거 (평일, 23:00 UTC)
2. Orchestrator가 병렬로 수집:
   - DART: 전일 15:30 ~ 당일 08:00 전체 공시
   - 네이버: 수집된 종목 + 코어종목 뉴스
   - ECOS: 환율·지표
3. ImportanceClassifier: 공시/뉴스를 **높음/중간/낮음** 분류 (가드레일 프롬프트)
4. ReportBuilder: 섹션 조립 → 금지어 후처리 필터 → 면책 문구 부착
5. Notifier: 디스코드 + 텔레그램 동시 발송
6. (선택) StorageInterface: 리포트 아카이브

---

## 7. 리포트 포맷 (초안)

```
📊 StockBriefing — 2026.05.30 (금) 08:00

━━ 시장 분위기 ━━
• 코스피 2,650 / 코스닥 860
• 원/달러 1,365원
• WTI 유가 $78

━━ 🔴 중요도 높음 (오늘 주목할 재료) ━━
[삼성전자] 공급계약 체결 공시
  └ 요약: ...
  └ 출처: dart.fss.or.kr/...

[OO바이오] 임상 3상 결과 공시
  └ ...

━━ 🟡 중요도 중간 ━━
• ...

━━ ⭐ 내 관심종목 ━━
[코어종목 A] 관련 뉴스 2건 / 공시 없음
  └ ...

──────────────────
⚠️ 본 리포트는 공개된 공시·뉴스를 정리·분류한 정보 제공
   목적의 자료이며, 투자 자문이나 매매 추천이 아닙니다.
   투자 판단과 책임은 본인에게 있습니다.
```

---

## 8. 기술 스택

| 영역 | 선택 | 근거 |
|---|---|---|
| 언어 | Python | 생태계/크롤링/LLM SDK |
| LLM | Claude **Sonnet**(분류·요약) + **Haiku**(단순 정제) | 품질/비용 최적화 |
| 스케줄러 | GitHub Actions cron | 무료, 무상태 환경에 적합 |
| 발송 | Discord Webhook + Telegram Bot | 둘 다, Notifier 추상화 |
| API 레이어 | 얇은 FastAPI | 수동 트리거/조회/헬스체크 (과설계 방지) |
| 시크릿 | `.env` / GitHub Secrets | 키 노출 방지 |
| 저장 | StorageInterface (MVP no-op) | 향후 Postgres 확장 여지 |

---

## 9. 4주 일정

| 주차 | 목표 | 산출물 |
|---|---|---|
| **Week 1** | 데이터 수집 레이어 | DART/네이버/ECOS 클라이언트, 시간창 공시 스캔, watchlist.yaml 로딩, 정규화 모델 |
| **Week 2** | 분석 레이어 | ImportanceClassifier(가드레일 프롬프트), 요약, ReportBuilder, 금지어 필터, 면책 |
| **Week 3** | 발송 + 자동화 | Notifier 추상화(디스코드/텔레그램), GitHub Actions 스케줄, 얇은 FastAPI |
| **Week 4** | 다듬기 | 실사용 피드백 반영, 프롬프트 튜닝, 에러처리/로깅, README·아키텍처 문서, 면접 데모 정리 |

> 본인이 매일 받아 사용 = 즉시 피드백 루프. Week 1 후반부터 부분 발송 시작해 4주 내내 실사용 권장.

---

## 10. 면접 어필 포인트

1. **실사용 동기** — 본인이 매일 쓰는 도구, 실제 페인에서 출발
2. **멀티 API 통합** — DART/네이버/ECOS 3종 + 시크릿 관리
3. **LLM 비용 최적화** — Sonnet/Haiku 역할 분리
4. **법적 리스크의 시스템적 통제** — 분류 스키마 + 가드레일 + 후처리 필터
5. **자동화 파이프라인** — GitHub Actions 무상태 cron, 시간창 기반 무DB 설계
6. **확장 가능 설계** — Notifier/Storage 인터페이스 추상화
7. **엔지니어링 판단력** — "필요 없는 인프라(DB/웹서버)를 무리하게 넣지 않은" 절제

---

## 11. 리스크 & 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 네이버 크롤링/API 차단·한도 | 뉴스 누락 | 정식 검색 API 사용(크롤링 회피), 호출량 캡, 실패 시 graceful degrade |
| DART API 스키마/한도 변경 | 수집 실패 | 클라이언트 레이어 격리, 스키마 검증, 일일 한도 내 설계 |
| LLM 가드레일 우회(추천성 출력) | 법적 리스크 | 프롬프트 + 후처리 금지어 필터 2중, 분류 대상 스키마 고정 |
| GitHub Actions 무상태 | 상태 유실 | 시간창 기반 stateless 설계로 회피 |
| LLM 비용 초과 | 운영비 | Haiku 분담, 입력 토큰 캡, 공시 사전 필터링 |
| 발송 실패(웹훅/봇 다운) | 미수신 | 채널 이중화(디스코드+텔레그램), 실패 로깅/재시도 |
| 공시 과다일 정보 폭주 | 가독성 저하 | 중요도 높음 우선 노출, 중간/낮음 접기, 상위 N개 캡 |

---

## 12. 백로그 / 확장 아이디어

- KIS API 연동 (전일 등락률·거래량)
- 무료 호스팅 Postgres(Supabase/Neon)로 리포트 이력 저장 + 간단 분석
- 주식 TV 프로그램 음성 → STT → 요약
- 리포트 웹 대시보드 (FastAPI 확장)
- 미국장/선물 야간 동향 섹션
- 종목별 과거 공시 트렌드 분석

---

## 13. 면책

본 시스템이 생성하는 모든 리포트는 공개된 공시·뉴스를 **정리·분류한 정보 제공** 목적의 자료이며, **투자 자문이나 매매 추천이 아니다.** 투자 판단과 그 책임은 전적으로 사용자 본인에게 있다.
